# Results ARE checked in: a table nobody can rescore is a table you have to take
# on trust, so every published number ships with the run that produced it.
# results-<arm>.jsonl are the per-arm runs, results.jsonl is them concatenated.
__pycache__/
